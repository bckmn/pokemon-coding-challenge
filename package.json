{
  "name": "astra-code-test",
  "private": true,
  "scripts": {
    "build": "npm run build --workspaces",
    "start": "npm run start --workspace=api"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "rimraf": "^6.0.1",
    "typescript": "^5.7.2"
  }
}
